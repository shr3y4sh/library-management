import { generateAccToken, generateRefToken } from './generate.tokens.js';
import { verifyAccessToken, verifyRefreshToken } from './verify.tokens.js';

//
export default {
    generateAccToken,
    generateRefToken,
    verifyAccessToken,
    verifyRefreshToken,
};
